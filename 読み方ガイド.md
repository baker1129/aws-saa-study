# 読み方ガイド

## CLF と SAA の思考の違い

| | CLF | SAA |
|---|---|---|
| 問い | このサービスは何か？ | なぜこのサービスを選ぶか？ |
| 視点 | 説明できる | 判断できる |

CLFで「S3はオブジェクトストレージ」と知っている。  
SAAでは「この要件ならS3かEBSかEFSか、なぜか」を答える。  
**知識から判断へ**の切り替えが SAA の本質。

---

## 読むときに頭に置く3つの問い

何かを読むたび、この3つを自問する。

### ① なぜこのサービスは存在するか
「これがないと何が困るか」を考える。  
困る理由がわかると、使うべき場面が自然に見えてくる。

### ② 似たサービスと何が違うか
SAAの選択肢は「どれも一見正しそう」な形で出る。  
ElastiCacheとDAX、SNSとSQS、マルチAZとリードレプリカ——これらの差を言えるかが勝負。

### ③ 問題文にどんな言葉があればこれを選ぶか
試験には選択肢を絞るキーワードが必ず出る。  
「高可用性」「コスト最小」「サーバーレス」「中断されてもよい」など。  
読みながら「このサービスに対応するキーワードは何か」を意識する。

---

## ① がわからないときの代替

「これがないと困るか」が浮かばないのは、システムを作ったことがないから想像できないだけで、理解力の問題ではない。

**代わりにこう問う：「このサービスが生まれる前、人々は何をしていたか」**

| サービス | 生まれる前に起きていたこと |
|---|---|
| Auto Scaling | サーバーが足りなくなったら手動でEC2を追加していた。夜中のアクセス急増に誰かが気づくまでサービスが落ちた |
| Secrets Manager | DBパスワードをソースコードに直書きしていた。GitHubに上がって漏れた |
| SQS | 注文が来たとき在庫システムに直接リクエストを投げていた。在庫システムが落ちたら注文も全部消えた |
| ELB | 1台のサーバーにアクセスが集中していた。そのサーバーが落ちたらサービスごと止まった |
| RDS Multi-AZ | DBサーバーが1台。障害が起きたら手動で復旧するまで止まり続けた |

「昔はこういう問題があった、だからこのサービスが作られた」という順番で読む。

---

## 理解の確認方法

読み終えたあと、ノートを閉じてこれを言えるか試す。

```
○○は、△△という問題を解決するために存在する。
□□と似ているが、〜という点で異なる。
試験では「（キーワード）」という言葉が出たときに選ぶ。
```

言えなかった部分 → `自分のメモ欄` に「まだわからない」と書く。  
言えた部分 → 自分の言葉でそのまま書き留める。

---

## 3つの問いの実例（マルチAZ）

- **① なぜ存在するか** → 1つのAZが落ちても自動で切り替わるため。手動対応なしにDBを生き続けさせる
- **② 似たサービスとの違い** → リードレプリカは読み込み性能向上のため。フェイルオーバーするのはマルチAZだけ
- **③ キーワード** → 「高可用性」「自動フェイルオーバー」「ダウンタイムを最小に」
